# Task: Scaffold Web UI Package (Phase 6)

**Status:** completed
**Created:** 2026-04-11
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
Phases 1-5 are complete. Phase 6 (Web UI) is the next phase per the implementation plan. The `packages/intelli-mock-ui/` directory doesn't exist yet. This task scaffolds the package with Lit Element + Vite infrastructure and implements the first component (Mock List) as a proof of concept.

## Scope
### In Scope
- [x] Create `packages/intelli-mock-ui/` with Lit Element + Vite + Material Web setup
- [x] Configure `package.json`, `tsconfig.json`, `vite.config.ts`
- [x] Add dependencies: `lit`, `@material/web`, `codemirror` (v6)
- [x] Implement API service (`src/services/api.ts`) for HTTP client
- [x] Implement Mock List component (`src/components/mock-list.ts`)
- [x] Wire up to workspace (root `pnpm-workspace.yaml` already configured)
- [x] Build passes: `pnpm -r build`

### Out of Scope
- Mock detail view, script editor, sample management, try-it panel, traffic viewer
- Authentication flow / JWT handling in UI
- Production deployment config

## Steps
1. Create package scaffold: `packages/intelli-mock-ui/` with `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`
2. Add dependencies: `lit`, `@material/web`, `vite` as dev dependencies
3. Create API service (`src/services/api.ts`) — fetch-based HTTP client for core API
4. Create Mock List component (`src/components/mock-list.ts`) — displays list of mocks from API
5. Create entry point (`src/index.ts`) — registers components, bootstraps app
6. Create `index.html` shell for the SPA
7. Run `pnpm -r build` to verify compilation
8. Run `pnpm -r typecheck` to verify types

## Acceptance Criteria
- [x] `packages/intelli-mock-ui/package.json` exists with correct dependencies
- [x] `packages/intelli-mock-ui/vite.config.ts` builds to `dist/`
- [x] `packages/intelli-mock-ui/src/services/api.ts` exists with mock CRUD methods
- [x] `packages/intelli-mock-ui/src/components/mock-list.ts` renders a list of mocks
- [x] `pnpm -r build` succeeds with exit code 0
- [x] `pnpm -r typecheck` succeeds with exit code 0

## References
- Related design: docs/ARCHITECT.md (Phase 6: Web UI, Monorepo Structure)
- Related PRD: docs/PRD.md (Phase 6: Web UI milestone, Functional Requirements #17)
- Progress: docs/PROGRESS.md (Phase 6: Web UI — 0/6 items, Not Started)
