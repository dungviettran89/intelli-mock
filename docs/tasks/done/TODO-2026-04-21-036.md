# Task: App Shell & Settings Playwright E2E Tests

**Status:** pending
**Created:** 2026-04-21
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
Recently, we completed the App Shell Layout and Settings Panel UI components (Tasks 034 & 035). We need to ensure that the layout renders correctly, navigation works via the sidebar, and the Settings panel functionality is visually intact and functional using our Playwright E2E testing infrastructure.

## Scope
### In Scope
- [ ] Create `packages/intelli-mock-ui/test/e2e/app-shell.spec.ts`.
- [ ] Create `packages/intelli-mock-ui/test/e2e/settings-panel.spec.ts`.
- [ ] Test the global navigation (Sidebar interactions: clicking Settings, Mocks, and verifying view changes).
- [ ] Test that the `app-shell` correctly wraps the views.
- [ ] Test that the settings panel inputs are usable and accessible in E2E.

### Out of Scope
- Creating new mockups or UI components.
- Mocking complex AI interactions within the E2E tests (just basic rendering).

## Steps
1. Create `app-shell.spec.ts` inside `packages/intelli-mock-ui/test/e2e/`.
2. Add a test to verify the app shell, sidebar, and header are attached to the DOM.
3. Add a test to verify navigation (e.g., clicking on the 'Settings' link changes the main view to `<settings-panel>`).
4. Create `settings-panel.spec.ts` inside `packages/intelli-mock-ui/test/e2e/`.
5. Add tests to ensure form elements like Provider, Model, and input fields exist and can be interacted with.
6. Run `pnpm test:e2e` inside `@intelli-mock/ui` (or root) to validate the new tests.

## Acceptance Criteria
- [ ] `app-shell.spec.ts` passes and validates layout & navigation state routing.
- [ ] `settings-panel.spec.ts` passes and validates rendering of settings controls.
- [ ] No regression in `mock-list.spec.ts`.

## References
- Related design: docs/UI.md (Layout Structure)
- Related PRD: docs/PRD.md (Web UI section)
- Playwright Setup: packages/intelli-mock-ui/test/e2e/setup.ts
