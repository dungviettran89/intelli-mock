# Task: Implement Settings Page UI Component

**Status:** pending
**Created:** 2026-04-21
**Priority:** medium

## Overview
Implement the `<settings-panel>` Web Component in `@intelli-mock/ui` using Lit Element and Material Web Components, based on the provided mockup (`docs/mockups/settings.html`).

## Acceptance Criteria
- Create `packages/intelli-mock-ui/src/components/settings-panel.ts` as a Lit Element (`@customElement('settings-panel')`).
- Implement the "Tenant Configuration" section (Slug, Name) and "Usage Metrics" card.
- Implement the "AI Orchestration" config section (Provider, Model, Temperature).
- Implement the "Security & Auth" config section (JWT Issuer, Algorithm).
- Implement the "Proxy Engine Defaults" section (Global Timeout, Cache TTL).
- Map the visual structure and layout from the Tailwind-based `settings.html` mockup to Lit/Vanilla CSS, maintaining the Red/Black/White theme specified in `docs/UI.md`.
- Use Material Web Components (`@material/web`) for form elements (text fields, selects, sliders, buttons) where appropriate.
- Create unit tests for rendering the component in `packages/intelli-mock-ui/test/components/settings-panel.test.ts`.
- Export the component from `packages/intelli-mock-ui/src/index.ts`.

## Implementation Details
- The mockup provides a complete static HTML/Tailwind representation. Extract the semantic structure and translate the Tailwind utility classes into component-scoped CSS.
- Ensure the layout is responsive, matching the grid behavior in the mockup (1 column on mobile, 2/3 columns on larger screens).

## Verification
- Build the UI package: `pnpm --filter @intelli-mock/ui build`
- Run UI unit tests: `pnpm test --filter @intelli-mock/ui`
- Verify 100% pass rate.
