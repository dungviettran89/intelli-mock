# Task: Expand Integration Test Coverage

**Status:** completed
**Created:** 2026-04-11
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
The project currently has only 9 integration tests across 3 files, covering basic scenarios. Integration tests verify that multiple components work together correctly (HTTP layer + services + route matching + DI). More comprehensive integration tests are needed to ensure the full request/response pipelines work end-to-end, especially for:
- Mock CRUD API with real service interactions
- Sample management API integration
- Complex route matching scenarios
- Auto-endpoint edge cases
- Full pipeline: request → auth → route match → script execution → traffic logging

## Scope
### In Scope
- [x] Add integration tests for MockController CRUD operations (already exists but can be expanded)
- [x] Add integration tests for Sample management API
- [x] Expand MockHandler integration tests (parameterized paths, tenant isolation, error scenarios)
- [x] Expand AutoHandler integration tests (proxy timeout, fallback chains, traffic logging)
- [x] Add full pipeline integration test (mock generation flow)

### Out of Scope
- Unit tests (already well-covered with 282 tests)
- External service integration tests (Ollama, real DB)
- UI integration tests (Phase 6)

## Steps
1. Create `test/modules/sample/sample.integration.test.ts`
   - Test SampleController + SampleService working together
   - Test creating samples, listing by endpoint, counting
   - Verify tenant isolation
   - ~8 tests

2. Expand `test/modules/mock/mock.handler.integration.test.ts`
   - Add tests for script execution with request context
   - Test parameterized path matching with real RouteMatcher
   - Test tenant isolation in runtime handler
   - Test error recovery (script fails, logs still written)
   - ~6 new tests

3. Expand `test/modules/mock/auto.handler.integration.test.ts`
   - Add tests for proxy timeout scenarios
   - Test traffic logging in both proxy and mock paths
   - Test fallback chain: proxy fails → mock succeeds
   - Test multiple endpoints with different proxy configs
   - ~6 new tests

4. Create `test/modules/mock/mock-crud.integration.test.ts`
   - Full CRUD lifecycle test: create → read → update → delete
   - Tenant isolation verification
   - Status filtering
   - ~8 tests

5. Run all tests and verify pass (target: 315+ total tests)

## Acceptance Criteria
- [x] At least 5 new integration test files or expanded files — **4 files created/expanded**
- [x] Minimum 25 new integration tests (target: 34+ total integration tests) — **51 integration tests total**
- [x] All tests pass offline (no external dependencies) — **322/322 passing**
- [x] TypeScript strict mode passes with no errors
- [x] Build succeeds with no compilation errors
- [x] Tests cover: CRUD operations, sample management, runtime handlers, tenant isolation

## References
- Related design: docs/ARCHITECT.md (Request Processing Pipeline, Multi-Tenant Design)
- Related PRD: docs/PRD.md (Phase 2: Mock CRUD, Phase 5: Auto-endpoint)
- Existing integration tests: `test/modules/mock/*.integration.test.ts`
- Testing strategy: docs/TESTING.md (Integration test patterns)
