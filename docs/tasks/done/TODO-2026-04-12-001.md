# Task: Traffic Log Retention Cron — Tests for RetentionCron + deleteOlderThan

**Status:** completed
**Created:** 2026-04-12
**Completed:** 2026-04-12
**Priority:** high
**Estimated Time:** 20-30 mins

## Context

Phase 7: Polish requires a traffic log retention mechanism. The source files (`retention-cron.ts` and `traffic-logger.ts`) are already implemented, and `TrafficService.deleteOlderThan()` exists. However, **tests were missing** for the retention cron service and the deleteOlderThan method. This task added the missing unit tests to complete the feature.

## Scope

### In Scope
- [x] Create `test/core/logging/retention-cron.test.ts` — Unit tests for RetentionCron service (21 tests)
- [x] Extend `test/modules/mock/traffic.service.test.ts` with `deleteOlderThan` tests (4 tests)

### Out of Scope
- TrafficLogger tests (already complete)
- TrafficLogger service implementation (already complete)
- RetentionCron service implementation (already complete)
- TrafficService.deleteOlderThan() implementation (already complete)
- UI traffic viewer (separate Phase 6 task)
- Swagger/OpenAPI docs (separate Phase 7 task)
- Docker image / CI/CD pipeline (separate Phase 7 tasks)

## Steps

1. **Write `deleteOlderThan()` tests** — Add ~4 tests to `traffic.service.test.ts` covering: successful deletion, zero rows deleted, tenant scoping, and error handling ✅
2. **Write RetentionCron tests** — Create `retention-cron.test.ts` with ~10 tests covering: start/stop lifecycle, immediate execution, scheduled intervals, cleanup logic, tenant iteration, error handling, and unref behavior ✅ (21 tests created)
3. **Run tests** — Ensure all new tests pass with `pnpm test` ✅ (396 tests pass)
4. **Verify coverage** — Ensure coverage thresholds are met ✅

## Acceptance Criteria
- [x] `test/core/logging/retention-cron.test.ts` exists with 21 tests (exceeded ~10 target)
- [x] `test/modules/mock/traffic.service.test.ts` includes 4 `deleteOlderThan` tests
- [x] All new tests pass with `pnpm test` (396 tests total, all passing)
- [x] Coverage thresholds met: 80% lines, 75% branches, 80% functions
- [x] `pnpm -r build` passes with no type errors
- [x] `pnpm -r typecheck` passes

## References
- Related design: `docs/ARCHITECT.md` — Phase 7: Polish (Traffic log retention cron)
- Related PRD: `docs/PRD.md` — Functional: "Traffic log viewer with 1-month retention policy"
- Service implementation: `packages/intelli-mock-core/src/core/logging/retention-cron.ts`
- Traffic service: `packages/intelli-mock-core/src/modules/mock/traffic.service.ts`
- Existing test patterns: `packages/intelli-mock-core/test/modules/mock/traffic.service.test.ts`
- Testing conventions: `docs/TESTING.md` — Service tests with mocked repositories
