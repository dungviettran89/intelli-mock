# Task: UI Component Unit Tests

**Status:** completed
**Created:** 2026-04-12
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
The `@intelli-mock/ui` package has 3 implemented components (mock-list, mock-detail, script-editor) but **zero unit tests**. According to PROGRESS.md, the UI package shows "0 test files" despite having implemented components. This task adds Vitest unit tests for all 3 components to ensure they render correctly and handle user interactions properly.

## Scope
### In Scope
- [x] Add Vitest config for UI package (jsdom environment)
- [x] Unit test for `mock-list.ts` component (rendering, API calls, item selection)
- [x] Unit test for `mock-detail.ts` component (loading state, data display, edit mode)
- [x] Unit test for `script-editor.ts` component (CodeMirror initialization, code changes, save action)
- [x] Test helpers for UI (mock API service, component fixtures)

### Out of Scope
- E2E Playwright tests (already exist in `test/e2e/`)
- Missing UI components (sample-editor, traffic-viewer, try-it)
- Integration tests with real backend

## Steps
1. ✅ Create Vitest config for UI package with jsdom environment
2. ✅ Create test helpers (mock API service, component render utilities)
3. ✅ Write unit tests for `mock-list.ts` — verify list rendering, API fetch calls, item click events
4. ✅ Write unit tests for `mock-detail.ts` — verify loading states, data binding, edit/save interactions
5. ✅ Write unit tests for `script-editor.ts` — verify CodeMirror setup, code change events, save callback
6. ✅ Run tests and verify coverage meets thresholds (80% lines, 75% branches)
7. ✅ Update PROGRESS.md to reflect new test count

## Acceptance Criteria
- [x] All 3 UI components have unit test files
- [x] Minimum 15 total tests across UI package (✅ 25 tests)
- [x] Tests run in < 5 seconds
- [x] Coverage thresholds pass (80% lines, 75% branches)
- [x] All tests pass with exit code 0
- [x] Tests are 100% offline (mocked API service)

## References
- Related design: docs/ARCHITECT.md (Phase 6: Web UI)
- Related PRD: docs/PRD.md (Phase 6: Web UI)
- Progress: docs/PROGRESS.md (Phase 6: Web UI 🟡 In Progress)
- Existing E2E tests: `packages/intelli-mock-ui/test/e2e/`
- Core testing patterns: `packages/intelli-mock-core/test/`
