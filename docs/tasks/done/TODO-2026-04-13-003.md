# Task: Add Missing Mock Controller Unit Tests

**Status:** completed
**Created:** 2026-04-13
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
The Mock Controller (`packages/intelli-mock-core/src/modules/mock/mock.controller.ts`) currently has 9 tests covering basic CRUD and generate operations. However, several edge cases and error paths are not tested:

1. Missing `id` parameter on GET/PUT/DELETE operations
2. Service errors during CRUD operations (500 responses)
3. Missing tenant ID handling (getTenantId throws)

Adding these tests will improve coverage and ensure all code paths in the controller are validated.

## Scope
### In Scope
- [x] Add test for missing `id` parameter on `GET /api/mocks/:id` (400 response)
- [x] Add test for missing `id` parameter on `PUT /api/mocks/:id` (400 response)
- [x] Add test for missing `id` parameter on `DELETE /api/mocks/:id` (400 response)
- [x] Add test for service error on `POST /api/mocks` (500 response)
- [x] Add test for service error on `GET /api/mocks` (500 response)
- [x] Add test for service error on `PUT /api/mocks/:id` (500 response)
- [x] Add test for service error on `DELETE /api/mocks/:id` (500 response)
- [x] Add test for missing id parameter on `POST /api/mocks/:id/generate` (400 response)
- [x] Run test suite to verify all tests pass

### Out of Scope
- Integration tests (already covered in mock.integration.test.ts)
- UI component tests
- Changes to source code

## Steps
1. Open `packages/intelli-mock-core/test/modules/mock/mock.controller.test.ts`
2. Add missing test cases to existing describe blocks or create new ones for error handling
3. Run `pnpm -r --filter @intelli-mock/core test` to verify all tests pass
4. Run `pnpm -r --filter @intelli-mock/core build` to ensure build succeeds

## Acceptance Criteria
- [x] At least 8 new test cases added to mock.controller.test.ts
- [x] All existing tests still pass
- [x] Core package builds successfully
- [x] Test file follows existing patterns and conventions (vi.mock, supertest, express app builder)

## References
- Related design: docs/ARCHITECT.md (Phase 2: Mock CRUD + Matching)
- Related PRD: docs/PRD.md (Functional Requirement #9: REST API for mock endpoint management)
- Current test file: `packages/intelli-mock-core/test/modules/mock/mock.controller.test.ts`
- Source file: `packages/intelli-mock-core/src/modules/mock/mock.controller.ts`
