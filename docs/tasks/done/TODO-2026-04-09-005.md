# Task: Phase 1 Unit Tests

**Status:** completed
**Created:** 2026-04-09
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
Phase 1 foundation code is complete (entities, database, auth, DI, app factory, server runner), but zero unit tests exist. The PRD requires 80% lines / 75% branches coverage before moving to Phase 2. This task creates the test infrastructure helpers and writes the 14 Phase 1 test files documented in `docs/TESTING.md`.

## Scope
### In Scope
- [ ] Create `test/helpers/test-app.ts` — Express app builder for supertest
- [ ] Create `test/helpers/jwt-utils.ts` — Offline JWT token generation for tests
- [ ] Write 14 test files covering config, entities, auth, database, app, container, server
- [ ] All tests must pass 100% offline with no external dependencies

### Out of Scope
- Phase 2 tests (route matcher, mock service, sample service)
- UI package tests
- CLI package tests

## Steps
1. Create `test/helpers/test-app.ts` — builds Express app with auth middleware, accepts test JWT tokens
2. Create `test/helpers/jwt-utils.ts` — generates offline JWT tokens using `jsonwebtoken` with RS256 (use test key pair or skip verification in tests)
3. Write entity tests (6 files) — verify TypeORM decorators, relationships, indexes, enums
4. Write config tests (2 files) — env loading, database config, CORS parsing
5. Write auth tests (2 files) — JWT middleware, user resolver
6. Write integration tests (4 files) — data source, app factory, container, server runner
7. Run `pnpm test` and verify all 14 tests pass with exit code 0

## Acceptance Criteria
- [x] All 14 test files exist and pass
- [x] `pnpm test` exits with code 0
- [x] Coverage meets thresholds: ≥80% lines, ≥75% branches, ≥80% functions
- [x] No external service calls — fully mocked/offline
- [x] Test helpers (`test-app.ts`, `jwt-utils.ts`) are reusable for Phase 2+

## References
- Related design: docs/ARCHITECT.md (Implementation Plan — Phase 1)
- Related PRD: docs/PRD.md (Testing Strategy, Requirements)
- Test file checklist: docs/TESTING.md (Test File Checklist section)
