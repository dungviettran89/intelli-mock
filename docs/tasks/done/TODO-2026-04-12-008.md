# Task: Sample Editor UI Component

**Status:** completed
**Created:** 2026-04-12
**Priority:** high
**Estimated Time:** 20-30 mins

## Context
Phase 6: Web UI is partially complete (4/6 items). The sample editor component is missing, which prevents users from managing sample request/response pairs through the UI. This is a core feature for the AI mock generation workflow, as users need to provide samples before AI can generate scripts.

## Scope
### In Scope
- [ ] Implement `sample-editor.ts` Lit Element component with Material Web UI
- [ ] Add sample list view with request/response pair display
- [ ] Add form to create/edit sample pairs
- [ ] Integrate with existing API service for CRUD operations
- [ ] Add unit tests for the component

### Out of Scope
- Backend API changes (already implemented)
- Sample validation logic (handled by backend)
- Bulk import/export features

## Steps
1. Read existing UI components (`mock-detail.ts`, `script-editor.ts`) to understand patterns and conventions
2. Read the API service (`services/api.ts`) to understand available sample endpoints
3. Implement `sample-editor.ts` component with:
   - Sample pair list display
   - Add/edit form with request/response fields
   - Delete functionality
   - Material Web components for UI
4. Add unit tests in `test/unit/components/sample-editor.test.ts`
5. Verify component follows existing Lit Element patterns
6. Run build and tests to ensure no regressions

## Acceptance Criteria
- [x] `sample-editor.ts` component implements full CRUD for sample pairs
- [x] Component uses Material Web components consistently with other UI components
- [x] Component integrates with existing API service
- [x] At least 5 unit tests covering component lifecycle and interactions
- [x] All existing tests still pass
- [x] TypeScript compilation succeeds with no errors

## References
- Related design: docs/ARCHITECT.md (Phase 6: Web UI)
- Related PRD: docs/PRD.md (Functional requirement #17)
- Progress: docs/PROGRESS.md (Phase 6: Web UI 🟡 In Progress)
- Existing components: `packages/intelli-mock-ui/src/components/mock-detail.ts`, `script-editor.ts`
- API service: `packages/intelli-mock-ui/src/services/api.ts`
